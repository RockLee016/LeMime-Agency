AOS.init({
    duration: 1000, //la durrée des animations
    easing: "ease-in-out", //le type d'accelaration
    // delay: 100, // delai par defaut (ms)
    once: false, // true = l'animation ne joue qu'une fois
    mirror: true // true = rejoue quand on remonte
});